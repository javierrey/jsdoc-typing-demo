// sys/types.js (NodeJS)
// @ts-check

/* Core: */

/**
 * @typedef {import('../view/core/types.js').PlainObject} PlainObject;
 * @typedef {import('../view/core/types.js').ArrayObject} ArrayObject;
 * @typedef {import('../view/core/types.js').FunctionObject} FunctionObject;
 * @typedef {import('../view/core/types.js').JsonValue} JsonValue;
 * @typedef {import('../view/core/types.js').JsonObject} JsonObject;
 * @typedef {import('../view/core/types.js').JsonArray} JsonArray;
 */

/* System: */

/**
 * Basic NodeJS types for generic data structures.
 * @typedef {import('node:http').IncomingMessage} IncomingMessage
 * @typedef {import('node:http').ServerResponse} ServerResponse
 * @typedef {import('node:buffer').Buffer} NodeBuffer
 * @typedef {import('node:stream').Readable} ReadableStream
 * @typedef {import('node:stream').Writable} WritableStream
 */

/* Custom: */

/* * */

// export default undefined; // module
